options = {
  :input_root  => File.expand_path('../app/assets/mapnik', __FILE__),
  :output_root => File.expand_path('../public/assets/mapnik', __FILE__)
}

guard 'slim', options do
  watch(%r{.*\.slim})
end

